1.Select clause: 
	Enter the fields separated by comma(,) without space. 
	Fields with operators, field1(space) operator(space) field2
2. Enter integer
3.Fields separated by comma(,) without spaces
4.Fields separated by comma(,) without spaces
5.Eg: state_1~=~"NY" && cust_1~<~cust && prod_1~=~prod
6. separated by space


Assumptions
1. we will not consider default table columns for aggregate fn, we must consider another grouping variable
ex: - Example-3

